此目录包含与自动生成的 CLI 文档相同的结构。

在所有自动生成的文件中，都注入了像 `description` 和 `example` 这样的代码片段。

如果你把一个文件放在与自动生成的配套文件相同的文件名和路径下，这些代码片段就会被注入到文件中。

例如，`docs/autogenerated/pixi.md` 是自动生成的文件，而 `docs/reference/cli/pixi.md` 是注入的文件。

剪裁的文档可以在这里找到：https://facelessuser.github.io/pymdown-extensions/extensions/snippets/
