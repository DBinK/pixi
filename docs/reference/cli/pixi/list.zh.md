---
title: pixi list
---
<!--- This file is autogenerated. Do not edit manually! -->
# [pixi](../pixi.md) list

列出当前工作区的包

--8<-- "docs/reference/cli/pixi/list_extender:description"

## 用法
```
pixi list [OPTIONS] [REGEX]
```

## 参数
- <a id="arg-<REGEX>" href="#arg-<REGEX>">`<REGEX>`</a>
:  只列出与正则表达式匹配的包

## 选项
- <a id="arg---platform" href="#arg---platform">`--platform <PLATFORM>`</a>
:  要列出包的平台。默认为当前平台
- <a id="arg---json" href="#arg---json">`--json`</a>
:  是否以 json 格式输出
- <a id="arg---sort-by" href="#arg---sort-by">`--sort-by <SORT_BY>`</a>
:  排序策略
<br>**default**: `name`
<br>**options**: `size`, `name`, `kind`
- <a id="arg---fields" href="#arg---fields">`--fields <FIELDS>`</a>
:  选择要显示的字段及其顺序（逗号分隔）
<br>May be provided more than once.
<br>**default**: `name, version, build, size, kind, source`
<br>**options**: `arch`, `build`, `build-number`, `constrains`, `depends`, `file-name`, `is-editable`, `kind`, `license`, `license-family`, `md5`, `name`, `noarch`, `platform`, `requested-spec`, `sha256`, `size`, `source`, `subdir`, `timestamp`, `track-features`, `url`, `version`
- <a id="arg---environment" href="#arg---environment">`--environment (-e) <ENVIRONMENT>`</a>
:  要列出包的环境。默认为默认环境
- <a id="arg---explicit" href="#arg---explicit">`--explicit (-x)`</a>
:  只列出在工作区中显式定义的包

## 更新选项
- <a id="arg---frozen" href="#arg---frozen">`--frozen`</a>
:  按 lockfile 中定义安装环境，如果 lockfile 与清单文件不同步则不更新 lockfile
<br>**env**: `PIXI_FROZEN`
- <a id="arg---locked" href="#arg---locked">`--locked`</a>
:  安装环境前检查 lockfile 是否是最新的，当 lockfile 与清单文件不同步时中止
<br>**env**: `PIXI_LOCKED`
- <a id="arg---no-install" href="#arg---no-install">`--no-install`</a>
:  不修改环境，只修改 lock-file

## 全局选项
- <a id="arg---manifest-path" href="#arg---manifest-path">`--manifest-path (-m) <MANIFEST_PATH>`</a>
:  `pixi.toml`、`pyproject.toml` 或工作区目录的路径
- <a id="arg---workspace" href="#arg---workspace">`--workspace (-w) <WORKSPACE>`</a>
:  工作区名称

## 描述
列出当前工作区的包

高亮显示的包是显式依赖。


--8<-- "docs/reference/cli/pixi/list_extender:example"