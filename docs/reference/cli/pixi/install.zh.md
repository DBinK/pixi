---
title: pixi install
---
<!--- This file is autogenerated. Do not edit manually! -->
# [pixi](../pixi.md) install

安装环境，同时更新 lockfile 并安装环境

--8<-- "docs/reference/cli/pixi/install_extender:description"

## 用法
```
pixi install [OPTIONS]
```

## 选项
- <a id="arg---environment" href="#arg---environment">`--environment (-e) <ENVIRONMENT>`</a>
:  要安装的环境
<br>May be provided more than once.
- <a id="arg---all" href="#arg---all">`--all (-a)`</a>
:  安装所有环境
- <a id="arg---skip" href="#arg---skip">`--skip <SKIP>`</a>
:  跳过安装 lockfile 中存在的特定包。这是软排除：包会被跳过但其依赖会被安装
<br>May be provided more than once.
- <a id="arg---skip-with-deps" href="#arg---skip-with-deps">`--skip-with-deps <SKIP_WITH_DEPS>`</a>
:  跳过包及其整个依赖子树。这是硬排除：包及其依赖不会被安装，除非从另一个非跳过根可达
<br>May be provided more than once.
- <a id="arg---only" href="#arg---only">`--only <ONLY>`</a>
:  仅安装和构建这些包及其依赖。可以多次传递
<br>May be provided more than once.

## Config 选项
- <a id="arg---auth-file" href="#arg---auth-file">`--auth-file <AUTH_FILE>`</a>
:  包含认证 token 的文件路径
- <a id="arg---concurrent-downloads" href="#arg---concurrent-downloads">`--concurrent-downloads <CONCURRENT_DOWNLOADS>`</a>
:  最大并发网络请求数，默认 `50`
- <a id="arg---concurrent-solves" href="#arg---concurrent-solves">`--concurrent-solves <CONCURRENT_SOLVES>`</a>
:  最大并发求解数，默认值为 CPU 数量
- <a id="arg---pinning-strategy" href="#arg---pinning-strategy">`--pinning-strategy <PINNING_STRATEGY>`</a>
:  设置版本锁定策略
<br>**options**: `semver`, `minor`, `major`, `latest-up`, `exact-version`, `no-pin`
- <a id="arg---pypi-keyring-provider" href="#arg---pypi-keyring-provider">`--pypi-keyring-provider <PYPI_KEYRING_PROVIDER>`</a>
:  指定是否使用 keyring 查找 PyPI 凭证
<br>**options**: `disabled`, `subprocess`
- <a id="arg---run-post-link-scripts" href="#arg---run-post-link-scripts">`--run-post-link-scripts`</a>
:  运行 post-link 脚本（不安全）
- <a id="arg---tls-no-verify" href="#arg---tls-no-verify">`--tls-no-verify`</a>
:  不验证服务器的 TLS 证书
- <a id="arg---tls-root-certs" href="#arg---tls-root-certs">`--tls-root-certs <TLS_ROOT_CERTS>`</a>
:  使用哪个 TLS 根证书：'webpki'（捆绑的 Mozilla 根证书）、'native'（系统存储）或 'all'（两者）
<br>**env**: `PIXI_TLS_ROOT_CERTS`
- <a id="arg---use-environment-activation-cache" href="#arg---use-environment-activation-cache">`--use-environment-activation-cache`</a>
:  使用环境激活缓存（实验性）

## 更新选项
- <a id="arg---frozen" href="#arg---frozen">`--frozen`</a>
:  按 lockfile 中定义安装环境，如果 lockfile 与清单文件不同步则不更新 lockfile
<br>**env**: `PIXI_FROZEN`
- <a id="arg---locked" href="#arg---locked">`--locked`</a>
:  安装环境前检查 lockfile 是否是最新的，当 lockfile 与清单文件不同步时中止
<br>**env**: `PIXI_LOCKED`

## 全局选项
- <a id="arg---manifest-path" href="#arg---manifest-path">`--manifest-path (-m) <MANIFEST_PATH>`</a>
:  `pixi.toml`、`pyproject.toml` 或工作区目录的路径
- <a id="arg---workspace" href="#arg---workspace">`--workspace (-w) <WORKSPACE>`</a>
:  工作区名称

## 描述
安装环境，同时更新 lockfile 并安装环境。

此命令会安装环境，如果 lockfile 不是最新的，它将被更新。

`pixi install` 一次只能安装一个环境，如果您有多个环境，可以使用 `--environment` 标志选择正确的环境。如果您不提供环境，将安装 `default` 环境。

如果您想安装所有环境，可以使用 `--all` 标志。

运行 `pixi install` 不是运行其他命令（如 `pixi run` 或 `pixi shell`）的前提条件。这些命令会在环境尚未安装时自动安装环境。

您可以使用 `pixi reinstall` 重新安装所有环境、一个环境或仅安装环境的某些包。


--8<-- "docs/reference/cli/pixi/install_extender:example"